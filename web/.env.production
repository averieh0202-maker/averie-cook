# GitHub Pages production build (cross-origin). Cloudflare Pages uses `.env.pages`
# with an empty VITE_API_BASE_URL so the browser calls same-origin `/api`.
VITE_API_BASE_URL=https://averie-cook-api.averieh0202.workers.dev
